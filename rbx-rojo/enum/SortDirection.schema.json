{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SortDirection",
    "description": "SortDirection",
    "type": "object",
    "properties": {
        "SortDirection": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Ascending",
                "Descending"
            ]
        }
    }
}