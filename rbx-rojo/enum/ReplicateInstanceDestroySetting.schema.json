{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ReplicateInstanceDestroySetting",
    "description": "ReplicateInstanceDestroySetting",
    "type": "object",
    "properties": {
        "ReplicateInstanceDestroySetting": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}