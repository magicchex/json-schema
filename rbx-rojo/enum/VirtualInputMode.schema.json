{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VirtualInputMode",
    "description": "VirtualInputMode",
    "type": "object",
    "properties": {
        "VirtualInputMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "None",
                "Recording",
                "Playing"
            ]
        }
    }
}