{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "TweenStatus",
    "description": "TweenStatus",
    "type": "object",
    "properties": {
        "TweenStatus": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Canceled",
                "Completed"
            ]
        }
    }
}