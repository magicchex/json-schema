{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RestPose",
    "description": "RestPose",
    "type": "object",
    "properties": {
        "RestPose": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "RotationsReset",
                "Custom"
            ]
        }
    }
}