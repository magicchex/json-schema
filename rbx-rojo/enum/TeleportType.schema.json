{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "TeleportType",
    "description": "TeleportType",
    "type": "object",
    "properties": {
        "TeleportType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "ToPlace",
                "ToInstance",
                "ToReservedServer",
                "ToVIPServer",
                "ToInstanceBack"
            ]
        }
    }
}