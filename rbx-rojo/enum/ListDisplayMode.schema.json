{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ListDisplayMode",
    "description": "ListDisplayMode",
    "type": "object",
    "properties": {
        "ListDisplayMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Horizontal",
                "Vertical"
            ]
        }
    }
}