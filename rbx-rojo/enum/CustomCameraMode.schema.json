{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CustomCameraMode",
    "description": "CustomCameraMode",
    "type": "object",
    "properties": {
        "CustomCameraMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Classic",
                "Follow"
            ]
        }
    }
}