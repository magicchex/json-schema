{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "FilterResult",
    "description": "FilterResult",
    "type": "object",
    "properties": {
        "FilterResult": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Accepted",
                "Rejected"
            ]
        }
    }
}