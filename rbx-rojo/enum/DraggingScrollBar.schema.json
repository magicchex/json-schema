{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DraggingScrollBar",
    "description": "DraggingScrollBar",
    "type": "object",
    "properties": {
        "DraggingScrollBar": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "None",
                "Horizontal",
                "Vertical"
            ]
        }
    }
}