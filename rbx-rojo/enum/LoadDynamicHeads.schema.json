{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LoadDynamicHeads",
    "description": "LoadDynamicHeads",
    "type": "object",
    "properties": {
        "LoadDynamicHeads": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}