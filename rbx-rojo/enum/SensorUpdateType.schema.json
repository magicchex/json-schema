{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SensorUpdateType",
    "description": "SensorUpdateType",
    "type": "object",
    "properties": {
        "SensorUpdateType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "OnRead",
                "Manual"
            ]
        }
    }
}