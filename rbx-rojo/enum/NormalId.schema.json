{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "NormalId",
    "description": "NormalId",
    "type": "object",
    "properties": {
        "NormalId": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Right",
                "Top",
                "Back",
                "Left",
                "Bottom",
                "Front"
            ]
        }
    }
}