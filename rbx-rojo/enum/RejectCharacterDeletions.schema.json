{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RejectCharacterDeletions",
    "description": "RejectCharacterDeletions",
    "type": "object",
    "properties": {
        "RejectCharacterDeletions": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}