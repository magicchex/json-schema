{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RenderPriority",
    "description": "RenderPriority",
    "type": "object",
    "properties": {
        "RenderPriority": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "First",
                "Input",
                "Camera",
                "Character",
                "Last"
            ]
        }
    }
}