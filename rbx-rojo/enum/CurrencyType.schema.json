{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CurrencyType",
    "description": "CurrencyType",
    "type": "object",
    "properties": {
        "CurrencyType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Robux",
                "Tix"
            ]
        }
    }
}