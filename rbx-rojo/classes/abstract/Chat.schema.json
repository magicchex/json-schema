{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Chat",
    "description": "Chat",
    "type": "object",
    "properties": {
        "ClassName": {
            "const": "Chat"
        },
        "Properties": {
            "$ref": "#/definitions/Properties"
        }
    },
    "required": [
        "ClassName"
    ],
    "definitions": {
        "Properties": {
            "type": "object",
            "description": "Chat properties",
            "properties": {
                "Attributes": {
                    "$ref": "../../datatypes/Attribute.schema.json"
                },
                "BubbleChatEnabled": {
                    "type": "boolean",
                    "description": "boolean"
                },
                "LoadDefaultChat": {
                    "type": "boolean",
                    "description": "boolean"
                }
            }
        },
        "Meta": {
            "type": "object",
            "properties": {
                "className": {
                    "const": "Chat"
                },
                "properties": {
                    "$ref": "#/definitions/Properties"
                }
            }
        },
        "Project": {
            "properties": {
                "$className": {
                    "const": "Chat",
                    "description": "Chat"
                },
                "$properties": {
                    "$ref": "#/definitions/Properties"
                }
            },
            "required": [
                "$className"
            ]
        }
    }
}