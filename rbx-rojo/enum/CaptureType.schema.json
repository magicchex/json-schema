{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CaptureType",
    "description": "CaptureType",
    "type": "object",
    "properties": {
        "CaptureType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Screenshot",
                "Video"
            ]
        }
    }
}