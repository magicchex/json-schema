{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ContextActionPriority",
    "description": "ContextActionPriority",
    "type": "object",
    "properties": {
        "ContextActionPriority": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Low",
                "Medium",
                "High"
            ]
        }
    }
}