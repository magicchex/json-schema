{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "WebSocketState",
    "description": "WebSocketState",
    "type": "object",
    "properties": {
        "WebSocketState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Connecting",
                "Open",
                "Closing",
                "Closed"
            ]
        }
    }
}