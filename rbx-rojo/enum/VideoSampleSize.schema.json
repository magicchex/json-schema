{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VideoSampleSize",
    "description": "VideoSampleSize",
    "type": "object",
    "properties": {
        "VideoSampleSize": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Small",
                "Medium",
                "Large",
                "Full"
            ]
        }
    }
}