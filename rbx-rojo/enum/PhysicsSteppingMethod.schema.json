{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "PhysicsSteppingMethod",
    "description": "PhysicsSteppingMethod",
    "type": "object",
    "properties": {
        "PhysicsSteppingMethod": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Fixed",
                "Adaptive"
            ]
        }
    }
}