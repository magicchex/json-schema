{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RunContext",
    "description": "RunContext",
    "type": "object",
    "properties": {
        "RunContext": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Legacy",
                "Server",
                "Client",
                "Plugin"
            ]
        }
    }
}