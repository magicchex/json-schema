{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RuntimeUndoBehavior",
    "description": "RuntimeUndoBehavior",
    "type": "object",
    "properties": {
        "RuntimeUndoBehavior": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Aggregate",
                "Snapshot",
                "Hybrid"
            ]
        }
    }
}