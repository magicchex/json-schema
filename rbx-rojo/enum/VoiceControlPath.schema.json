{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VoiceControlPath",
    "description": "VoiceControlPath",
    "type": "object",
    "properties": {
        "VoiceControlPath": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Publish",
                "Subscribe",
                "Join"
            ]
        }
    }
}