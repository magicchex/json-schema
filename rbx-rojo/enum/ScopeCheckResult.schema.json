{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ScopeCheckResult",
    "description": "ScopeCheckResult",
    "type": "object",
    "properties": {
        "ScopeCheckResult": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "ConsentAccepted",
                "InvalidScopes",
                "Timeout",
                "NoUserInput",
                "BackendError",
                "UnexpectedError",
                "InvalidArgument",
                "ConsentDenied"
            ]
        }
    }
}