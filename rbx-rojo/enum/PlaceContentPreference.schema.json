{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "PlaceContentPreference",
    "description": "PlaceContentPreference",
    "type": "object",
    "properties": {
        "PlaceContentPreference": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "None",
                "All",
                "MentionsAndReplies",
                "Unknown"
            ]
        }
    }
}