{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SignalBehavior",
    "description": "SignalBehavior",
    "type": "object",
    "properties": {
        "SignalBehavior": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Immediate",
                "Deferred",
                "AncestryDeferred"
            ]
        }
    }
}