{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "TopBottom",
    "description": "TopBottom",
    "type": "object",
    "properties": {
        "TopBottom": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Top",
                "Center",
                "Bottom"
            ]
        }
    }
}