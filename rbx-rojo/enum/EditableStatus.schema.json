{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "EditableStatus",
    "description": "EditableStatus",
    "type": "object",
    "properties": {
        "EditableStatus": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Unknown",
                "Allowed",
                "Disallowed"
            ]
        }
    }
}