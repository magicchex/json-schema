{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RotationType",
    "description": "RotationType",
    "type": "object",
    "properties": {
        "RotationType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "MovementRelative",
                "CameraRelative"
            ]
        }
    }
}