{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ThrottlingPriority",
    "description": "ThrottlingPriority",
    "type": "object",
    "properties": {
        "ThrottlingPriority": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Extreme",
                "ElevatedOnServer"
            ]
        }
    }
}