{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "EasingDirection",
    "description": "EasingDirection",
    "type": "object",
    "properties": {
        "EasingDirection": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "In",
                "Out",
                "InOut"
            ]
        }
    }
}