{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "PredictionMode",
    "description": "PredictionMode",
    "type": "object",
    "properties": {
        "PredictionMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Automatic",
                "On",
                "Off"
            ]
        }
    }
}