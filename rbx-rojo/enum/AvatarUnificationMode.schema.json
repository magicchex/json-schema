{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "AvatarUnificationMode",
    "description": "AvatarUnificationMode",
    "type": "object",
    "properties": {
        "AvatarUnificationMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}