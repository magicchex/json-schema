{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CageType",
    "description": "CageType",
    "type": "object",
    "properties": {
        "CageType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Inner",
                "Outer"
            ]
        }
    }
}