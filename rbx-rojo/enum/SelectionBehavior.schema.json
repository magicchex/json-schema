{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SelectionBehavior",
    "description": "SelectionBehavior",
    "type": "object",
    "properties": {
        "SelectionBehavior": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Escape",
                "Stop"
            ]
        }
    }
}