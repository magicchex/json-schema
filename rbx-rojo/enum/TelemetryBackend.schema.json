{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "TelemetryBackend",
    "description": "TelemetryBackend",
    "type": "object",
    "properties": {
        "TelemetryBackend": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "UNSPECIFIED",
                "EventIngest",
                "Points",
                "Teletune",
                "EphemeralCounter",
                "EphemeralStat",
                "Counter",
                "Stat"
            ]
        }
    }
}