{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DialogTone",
    "description": "DialogTone",
    "type": "object",
    "properties": {
        "DialogTone": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Neutral",
                "Friendly",
                "Enemy"
            ]
        }
    }
}