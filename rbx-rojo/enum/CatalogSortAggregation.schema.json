{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CatalogSortAggregation",
    "description": "CatalogSortAggregation",
    "type": "object",
    "properties": {
        "CatalogSortAggregation": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Past12Hours",
                "PastDay",
                "Past3Days",
                "PastWeek",
                "PastMonth",
                "AllTime"
            ]
        }
    }
}