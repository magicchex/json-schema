{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DebuggerEndReason",
    "description": "DebuggerEndReason",
    "type": "object",
    "properties": {
        "DebuggerEndReason": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "ClientRequest",
                "Timeout",
                "InvalidHost",
                "Disconnected",
                "ServerShutdown",
                "ServerProtocolMismatch",
                "ConfigurationFailed",
                "RpcError"
            ]
        }
    }
}