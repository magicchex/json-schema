{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "GameSettings",
    "description": "GameSettings",
    "type": "object",
    "properties": {
        "ClassName": {
            "const": "GameSettings"
        },
        "Properties": {
            "$ref": "#/definitions/Properties"
        }
    },
    "required": [
        "ClassName"
    ],
    "definitions": {
        "Properties": {
            "type": "object",
            "description": "GameSettings properties",
            "properties": {
                "Attributes": {
                    "$ref": "../../datatypes/Attribute.schema.json"
                },
                "VideoCaptureEnabled": {
                    "type": "boolean",
                    "description": "boolean"
                },
                "VideoRecording": {
                    "type": "boolean",
                    "description": "boolean"
                }
            }
        },
        "Meta": {
            "type": "object",
            "properties": {
                "className": {
                    "const": "GameSettings"
                },
                "properties": {
                    "$ref": "#/definitions/Properties"
                }
            }
        },
        "Project": {
            "properties": {
                "$className": {
                    "const": "GameSettings",
                    "description": "GameSettings"
                },
                "$properties": {
                    "$ref": "#/definitions/Properties"
                }
            },
            "required": [
                "$className"
            ]
        }
    }
}