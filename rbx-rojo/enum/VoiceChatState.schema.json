{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VoiceChatState",
    "description": "VoiceChatState",
    "type": "object",
    "properties": {
        "VoiceChatState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Idle",
                "Joining",
                "JoiningRetry",
                "Joined",
                "Leaving",
                "Ended",
                "Failed"
            ]
        }
    }
}