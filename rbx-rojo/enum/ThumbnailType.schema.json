{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ThumbnailType",
    "description": "ThumbnailType",
    "type": "object",
    "properties": {
        "ThumbnailType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "HeadShot",
                "AvatarBust",
                "AvatarThumbnail"
            ]
        }
    }
}