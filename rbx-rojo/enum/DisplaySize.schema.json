{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DisplaySize",
    "description": "DisplaySize",
    "type": "object",
    "properties": {
        "DisplaySize": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Small",
                "Medium",
                "Large"
            ]
        }
    }
}