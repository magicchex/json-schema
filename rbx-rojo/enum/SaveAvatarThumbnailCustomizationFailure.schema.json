{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SaveAvatarThumbnailCustomizationFailure",
    "description": "SaveAvatarThumbnailCustomizationFailure",
    "type": "object",
    "properties": {
        "SaveAvatarThumbnailCustomizationFailure": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "BadThumbnailType",
                "BadYRotDeg",
                "BadFieldOfViewDeg",
                "BadDistanceScale",
                "Other",
                "Throttled"
            ]
        }
    }
}