{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RolloutState",
    "description": "RolloutState",
    "type": "object",
    "properties": {
        "RolloutState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}