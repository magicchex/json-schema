{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "FramerateManagerMode",
    "description": "FramerateManagerMode",
    "type": "object",
    "properties": {
        "FramerateManagerMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Automatic",
                "On",
                "Off"
            ]
        }
    }
}