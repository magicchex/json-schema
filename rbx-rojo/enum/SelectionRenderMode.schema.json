{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SelectionRenderMode",
    "description": "SelectionRenderMode",
    "type": "object",
    "properties": {
        "SelectionRenderMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Outlines",
                "BoundingBoxes",
                "Both"
            ]
        }
    }
}