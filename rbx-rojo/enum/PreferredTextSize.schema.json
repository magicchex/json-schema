{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "PreferredTextSize",
    "description": "PreferredTextSize",
    "type": "object",
    "properties": {
        "PreferredTextSize": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Medium",
                "Large",
                "Larger",
                "Largest"
            ]
        }
    }
}