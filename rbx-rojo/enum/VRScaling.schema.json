{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VRScaling",
    "description": "VRScaling",
    "type": "object",
    "properties": {
        "VRScaling": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "World",
                "Off"
            ]
        }
    }
}