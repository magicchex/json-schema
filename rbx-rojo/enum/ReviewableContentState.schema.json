{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ReviewableContentState",
    "description": "ReviewableContentState",
    "type": "object",
    "properties": {
        "ReviewableContentState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Pending",
                "Completed",
                "Failed"
            ]
        }
    }
}