{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ScaleType",
    "description": "ScaleType",
    "type": "object",
    "properties": {
        "ScaleType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Stretch",
                "Slice",
                "Tile",
                "Fit",
                "Crop"
            ]
        }
    }
}