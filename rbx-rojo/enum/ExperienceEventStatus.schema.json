{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExperienceEventStatus",
    "description": "ExperienceEventStatus",
    "type": "object",
    "properties": {
        "ExperienceEventStatus": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Active",
                "Cancelled",
                "Moderated",
                "Unpublished",
                "Unknown"
            ]
        }
    }
}