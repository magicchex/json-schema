{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "StudioStyleGuideModifier",
    "description": "StudioStyleGuideModifier",
    "type": "object",
    "properties": {
        "StudioStyleGuideModifier": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Selected",
                "Pressed",
                "Disabled",
                "Hover"
            ]
        }
    }
}