{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ImageCombineType",
    "description": "ImageCombineType",
    "type": "object",
    "properties": {
        "ImageCombineType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "BlendSourceOver",
                "Overwrite",
                "Add",
                "Multiply",
                "AlphaBlend"
            ]
        }
    }
}