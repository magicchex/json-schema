{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "PlaybackState",
    "description": "PlaybackState",
    "type": "object",
    "properties": {
        "PlaybackState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Begin",
                "Delayed",
                "Playing",
                "Paused",
                "Completed",
                "Cancelled"
            ]
        }
    }
}