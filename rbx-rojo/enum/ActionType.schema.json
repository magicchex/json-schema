{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ActionType",
    "description": "ActionType",
    "type": "object",
    "properties": {
        "ActionType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Nothing",
                "Pause",
                "Lose",
                "Draw",
                "Win"
            ]
        }
    }
}