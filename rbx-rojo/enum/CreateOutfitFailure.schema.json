{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CreateOutfitFailure",
    "description": "CreateOutfitFailure",
    "type": "object",
    "properties": {
        "CreateOutfitFailure": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "InvalidName",
                "OutfitLimitReached",
                "Other"
            ]
        }
    }
}