{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "OperationType",
    "description": "OperationType",
    "type": "object",
    "properties": {
        "OperationType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Null",
                "Union",
                "Subtraction",
                "Intersection",
                "Primitive"
            ]
        }
    }
}