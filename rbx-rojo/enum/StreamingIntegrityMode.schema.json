{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "StreamingIntegrityMode",
    "description": "StreamingIntegrityMode",
    "type": "object",
    "properties": {
        "StreamingIntegrityMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "MinimumRadiusPause",
                "PauseOutsideLoadedArea"
            ]
        }
    }
}