{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExperienceAuthScope",
    "description": "ExperienceAuthScope",
    "type": "object",
    "properties": {
        "ExperienceAuthScope": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "DefaultScope",
                "CreatorAssetsCreate"
            ]
        }
    }
}