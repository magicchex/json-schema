{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ImageAlphaType",
    "description": "ImageAlphaType",
    "type": "object",
    "properties": {
        "ImageAlphaType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "LockCanvasAlpha",
                "LockCanvasColor"
            ]
        }
    }
}