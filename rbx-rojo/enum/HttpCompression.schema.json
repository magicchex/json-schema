{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "HttpCompression",
    "description": "HttpCompression",
    "type": "object",
    "properties": {
        "HttpCompression": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "None",
                "Gzip"
            ]
        }
    }
}