{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "TonemapperPreset",
    "description": "TonemapperPreset",
    "type": "object",
    "properties": {
        "TonemapperPreset": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Retro"
            ]
        }
    }
}