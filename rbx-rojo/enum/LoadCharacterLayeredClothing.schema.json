{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LoadCharacterLayeredClothing",
    "description": "LoadCharacterLayeredClothing",
    "type": "object",
    "properties": {
        "LoadCharacterLayeredClothing": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}