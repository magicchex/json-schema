{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ForceLimitMode",
    "description": "ForceLimitMode",
    "type": "object",
    "properties": {
        "ForceLimitMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Magnitude",
                "PerAxis"
            ]
        }
    }
}