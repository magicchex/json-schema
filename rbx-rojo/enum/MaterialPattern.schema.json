{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "MaterialPattern",
    "description": "MaterialPattern",
    "type": "object",
    "properties": {
        "MaterialPattern": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Regular",
                "Organic"
            ]
        }
    }
}