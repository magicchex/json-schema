{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RunState",
    "description": "RunState",
    "type": "object",
    "properties": {
        "RunState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Stopped",
                "Running",
                "Paused"
            ]
        }
    }
}