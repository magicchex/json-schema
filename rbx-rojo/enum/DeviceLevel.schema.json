{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DeviceLevel",
    "description": "DeviceLevel",
    "type": "object",
    "properties": {
        "DeviceLevel": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Low",
                "Medium",
                "High"
            ]
        }
    }
}