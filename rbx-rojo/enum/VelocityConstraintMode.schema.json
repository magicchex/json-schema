{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VelocityConstraintMode",
    "description": "VelocityConstraintMode",
    "type": "object",
    "properties": {
        "VelocityConstraintMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Line",
                "Plane",
                "Vector"
            ]
        }
    }
}