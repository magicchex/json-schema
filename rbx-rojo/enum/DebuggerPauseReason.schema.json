{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DebuggerPauseReason",
    "description": "DebuggerPauseReason",
    "type": "object",
    "properties": {
        "DebuggerPauseReason": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Unknown",
                "Requested",
                "Breakpoint",
                "Exception",
                "SingleStep",
                "Entrypoint"
            ]
        }
    }
}