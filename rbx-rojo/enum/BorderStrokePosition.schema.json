{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "BorderStrokePosition",
    "description": "BorderStrokePosition",
    "type": "object",
    "properties": {
        "BorderStrokePosition": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Outer",
                "Center",
                "Inner"
            ]
        }
    }
}