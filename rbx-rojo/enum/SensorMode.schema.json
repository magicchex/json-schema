{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SensorMode",
    "description": "SensorMode",
    "type": "object",
    "properties": {
        "SensorMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Floor",
                "Ladder"
            ]
        }
    }
}