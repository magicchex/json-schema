{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "MouseBehavior",
    "description": "MouseBehavior",
    "type": "object",
    "properties": {
        "MouseBehavior": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "LockCenter",
                "LockCurrentPosition"
            ]
        }
    }
}