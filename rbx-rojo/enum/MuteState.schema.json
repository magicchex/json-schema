{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "MuteState",
    "description": "MuteState",
    "type": "object",
    "properties": {
        "MuteState": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Unmuted",
                "Muted"
            ]
        }
    }
}