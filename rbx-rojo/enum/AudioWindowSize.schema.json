{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "AudioWindowSize",
    "description": "AudioWindowSize",
    "type": "object",
    "properties": {
        "AudioWindowSize": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Small",
                "Medium",
                "Large"
            ]
        }
    }
}