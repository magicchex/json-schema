{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VolumetricAudio",
    "description": "VolumetricAudio",
    "type": "object",
    "properties": {
        "VolumetricAudio": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Disabled",
                "Automatic",
                "Enabled"
            ]
        }
    }
}