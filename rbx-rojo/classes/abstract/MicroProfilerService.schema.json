{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "MicroProfilerService",
    "description": "MicroProfilerService",
    "type": "object",
    "properties": {
        "ClassName": {
            "const": "MicroProfilerService"
        },
        "Properties": {
            "$ref": "#/definitions/Properties"
        }
    },
    "required": [
        "ClassName"
    ],
    "definitions": {
        "Properties": {
            "type": "object",
            "description": "MicroProfilerService properties",
            "properties": {
                "Attributes": {
                    "$ref": "../../datatypes/Attribute.schema.json"
                },
                "ContextLabel": {
                    "type": "string",
                    "description": "string"
                }
            }
        },
        "Meta": {
            "type": "object",
            "properties": {
                "className": {
                    "const": "MicroProfilerService"
                },
                "properties": {
                    "$ref": "#/definitions/Properties"
                }
            }
        },
        "Project": {
            "properties": {
                "$className": {
                    "const": "MicroProfilerService",
                    "description": "MicroProfilerService"
                },
                "$properties": {
                    "$ref": "#/definitions/Properties"
                }
            },
            "required": [
                "$className"
            ]
        }
    }
}