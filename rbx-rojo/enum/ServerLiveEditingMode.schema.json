{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ServerLiveEditingMode",
    "description": "ServerLiveEditingMode",
    "type": "object",
    "properties": {
        "ServerLiveEditingMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Uninitialized",
                "Enabled",
                "Disabled"
            ]
        }
    }
}