{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "OutfitSource",
    "description": "OutfitSource",
    "type": "object",
    "properties": {
        "OutfitSource": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "All",
                "Created",
                "Purchased"
            ]
        }
    }
}