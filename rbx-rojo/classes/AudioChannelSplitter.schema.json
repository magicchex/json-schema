{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "AudioChannelSplitter",
    "description": "AudioChannelSplitter",
    "type": "object",
    "properties": {
        "ClassName": {
            "const": "AudioChannelSplitter"
        },
        "Properties": {
            "$ref": "#/definitions/Properties"
        }
    },
    "required": [
        "ClassName"
    ],
    "definitions": {
        "Properties": {
            "type": "object",
            "description": "AudioChannelSplitter properties",
            "properties": {
                "Attributes": {
                    "$ref": "../datatypes/Attribute.schema.json"
                },
                "Layout": {
                    "$ref": "../enum/AudioChannelLayout.schema.json#/definitions/Enum",
                    "description": "Layout Enum"
                },
                "Archivable": {
                    "type": "boolean",
                    "description": "boolean"
                },
                "Name": {
                    "type": "string",
                    "description": "string"
                },
                "Sandboxed": {
                    "type": "boolean",
                    "description": "boolean"
                }
            }
        },
        "Meta": {
            "type": "object",
            "properties": {
                "className": {
                    "const": "AudioChannelSplitter"
                },
                "properties": {
                    "$ref": "#/definitions/Properties"
                }
            }
        },
        "Project": {
            "properties": {
                "$className": {
                    "const": "AudioChannelSplitter",
                    "description": "AudioChannelSplitter"
                },
                "$properties": {
                    "$ref": "#/definitions/Properties"
                }
            },
            "required": [
                "$className"
            ]
        }
    }
}