{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "AvatarContextMenuOption",
    "description": "AvatarContextMenuOption",
    "type": "object",
    "properties": {
        "AvatarContextMenuOption": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Friend",
                "Chat",
                "Emote",
                "InspectMenu"
            ]
        }
    }
}