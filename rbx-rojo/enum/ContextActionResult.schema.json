{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ContextActionResult",
    "description": "ContextActionResult",
    "type": "object",
    "properties": {
        "ContextActionResult": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Sink",
                "Pass"
            ]
        }
    }
}