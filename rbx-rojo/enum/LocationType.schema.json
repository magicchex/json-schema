{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LocationType",
    "description": "LocationType",
    "type": "object",
    "properties": {
        "LocationType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Character",
                "Camera",
                "ObjectPosition"
            ]
        }
    }
}