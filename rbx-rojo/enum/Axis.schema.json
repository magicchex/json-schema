{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Axis",
    "description": "Axis",
    "type": "object",
    "properties": {
        "Axis": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "X",
                "Y",
                "Z"
            ]
        }
    }
}