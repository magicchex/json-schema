{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "AudioSubType",
    "description": "AudioSubType",
    "type": "object",
    "properties": {
        "AudioSubType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Music",
                "SoundEffect"
            ]
        }
    }
}