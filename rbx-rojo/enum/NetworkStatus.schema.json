{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "NetworkStatus",
    "description": "NetworkStatus",
    "type": "object",
    "properties": {
        "NetworkStatus": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Unknown",
                "Connected",
                "Disconnected"
            ]
        }
    }
}