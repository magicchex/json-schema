{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExplosionType",
    "description": "ExplosionType",
    "type": "object",
    "properties": {
        "ExplosionType": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "NoCraters",
                "Craters"
            ]
        }
    }
}