{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "VirtualCursorMode",
    "description": "VirtualCursorMode",
    "type": "object",
    "properties": {
        "VirtualCursorMode": {
            "$ref": "#/definitions/Enum"
        }
    },
    "definitions": {
        "Enum": {
            "enum": [
                "Default",
                "Disabled",
                "Enabled"
            ]
        }
    }
}